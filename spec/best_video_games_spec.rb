require 'spec_helper'

describe BestVideoGames do
  it 'has a version number' do
    expect(BestVideoGames::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
